nelmio_api_doc:
    documentation:
        info:
            title: ApiNotFound
            description: Get Random pictures and messages with http code
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
